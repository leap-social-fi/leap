pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: "bun lint"
      stage_fixed: true
    - name: format
      run: "bun format"
      stage_fixed: true
    - name: check
      run: "bun check"
      stage_fixed: true
    - name: types
      run: "bun types"
      stage_fixed: true
